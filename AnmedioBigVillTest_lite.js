const test = new AnmedioBigVillTest({
  sticker: true || super(tizer.length),
  title__prog: test.amazingTest(),
  tiny: console.info(`Hello, ${user.name}`),
  size__shadowAllowFakeScreen: 120 / 120,
});
